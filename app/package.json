{
  "devDependencies": {
    "esbuild": "0.20.1"
  },
  "scripts": {
    "build:display": "esbuild --bundle --outfile=./dist/display.mjs --loader:.html=text --loader:.jpg=file ./src/display.js --platform=node --minify --format=esm",
    "build:collect": "esbuild --bundle --outfile=./dist/collect.mjs --loader:.html=text --loader:.jpg=file ./src/collect.js --platform=node --minify --format=esm"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.53"
  }
}
