{
  "devDependencies": {
    "esbuild": "0.20.1"
  },
  "scripts": {
    "build": "esbuild --bundle --outfile=./dist/bundle.js --loader:.html=text ./src/index.js --platform=node --minify --format=esm"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.53"
  }
}
